import { FC, memo } from 'react'
import HeaderComp from '../../componets/Header'

const Landing: FC = () => {
    return (
        <>
            <HeaderComp />
        </>
    )
}

export default memo(Landing)
